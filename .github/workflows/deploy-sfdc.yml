name: Deploy to Salesforce

on:
  push:
    branches:
      - main
    paths:
      - "sfdc/force-app/**"
      - "sfdc/sfdx-project.json"
      - ".github/workflows/deploy-sfdc.yml"

  workflow_dispatch:

concurrency:
  group: salesforce-deploy
  cancel-in-progress: true

jobs:
  validate-and-deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sfdc

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install npm dependencies
        run: npm install

      - name: Prettier check
        run: npm run prettier:verify

      - name: ESLint check
        run: npm run lint

      - name: Install Salesforce CLI
        run: |
          npm install -g @salesforce/cli
          sf version

      - name: Authenticate to Salesforce
        run: |
          echo "${{ secrets.SFDX_AUTH_URL }}" > ./SFDX_AUTH_URL.txt
          sf org login sfdx-url --sfdx-url-file ./SFDX_AUTH_URL.txt --alias deploy-org --set-default
          rm -f ./SFDX_AUTH_URL.txt

      - name: Abort scheduled jobs (prevent deploy conflicts)
        run: |
          sf apex run --target-org deploy-org -f <(echo "
          for (CronTrigger ct : [SELECT Id, CronJobDetail.Name FROM CronTrigger WHERE CronJobDetail.Name LIKE '%MarginArc%' OR CronJobDetail.Name LIKE '%Fulcrum%']) {
              System.abortJob(ct.Id);
          }
          ")

      - name: Validate deployment (dry run)
        run: |
          sf project deploy start \
            --target-org deploy-org \
            --source-dir force-app \
            --test-level RunSpecifiedTests \
            --tests MarginArcControllerTest MarginArcCompetitiveControllerTest MarginArcManagerControllerTest MarginArcBatchAnalyzerTest MarginArcAdminControllerTest MarginArcBackfillAnalyzerTest MarginArcSetupControllerTest MarginArcInstallHandlerTest MarginArcDemoDataLoaderTest MarginArcLicenseGateTest MarginArcDemoDataServiceTest MarginArcLicenseActivatorTest MarginArcLicenseValidatorTest MarginArcDealOutcomeSyncTest \
            --dry-run \
            --wait 20

      - name: Deploy to Salesforce
        run: |
          sf project deploy start \
            --target-org deploy-org \
            --source-dir force-app \
            --test-level RunSpecifiedTests \
            --tests MarginArcControllerTest MarginArcCompetitiveControllerTest MarginArcManagerControllerTest MarginArcBatchAnalyzerTest MarginArcAdminControllerTest MarginArcBackfillAnalyzerTest MarginArcSetupControllerTest MarginArcInstallHandlerTest MarginArcDemoDataLoaderTest MarginArcLicenseGateTest MarginArcDemoDataServiceTest MarginArcLicenseActivatorTest MarginArcLicenseValidatorTest MarginArcDealOutcomeSyncTest \
            --wait 20

      - name: Reschedule nightly jobs
        run: |
          sf apex run --target-org deploy-org -f <(echo "
          if ([SELECT COUNT() FROM CronTrigger WHERE CronJobDetail.Name LIKE '%MarginArc%Analyzer%' AND State = 'WAITING'] == 0) {
              System.schedule('MarginArc Nightly Analyzer', '0 0 2 * * ?', new MarginArcBatchAnalyzer());
          }
          if ([SELECT COUNT() FROM CronTrigger WHERE CronJobDetail.Name LIKE '%MarginArc%Outcome%' AND State = 'WAITING'] == 0) {
              System.schedule('MarginArc Deal Outcome Sync', '0 0 3 ? * SUN', new MarginArcDealOutcomeSync());
          }
          ")

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: deployment-logs
          path: sfdc/.sf/
          retention-days: 7
